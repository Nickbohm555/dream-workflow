0a. Study `AGENTS.md` to understand the operational rules.
0b. Study `IMPLEMENTATION_PLAN.md` if present to understand the current plan.
0c. Study the relevant project files before editing the plan.

1. Compare the current codebase or workflow docs against the intended outcome.
2. Create or update `IMPLEMENTATION_PLAN.md` as a prioritized list of remaining work.
3. For each section, derive concrete verification steps from the intended behavior.
4. Keep the plan focused on implementation order, not brainstorming.
5. At the end of the run, write one line to `.loop-commit-msg` describing the planning update.

NOTE: Keep this run in planning mode only. Do not implement features in this mode.
NOTE: Keep `AGENTS.md` operational only. Keep task state in `IMPLEMENTATION_PLAN.md`.

## Build & Run

Keep this file operational only. Use it for stable instructions the agent needs on every run:

- how to start the project
- how to restart services
- how to inspect logs
- how to run tests
- known environment requirements

If a command changes, update it here.

## Validation

Run these after implementation:

- Tests: `[test command]`
- Typecheck: `[typecheck command]`
- Lint: `[lint command]`

If a check is not applicable, replace it with `N/A`.

## Operational Notes

Capture durable runtime notes here:

- ports
- container names
- service names
- data reset commands
- debugging commands

Do not keep task lists or current work items here.

## Codebase Patterns

Capture conventions the agent should reuse:

- source roots
- shared utility locations
- naming conventions
- existing architectural patterns

Keep this concise and current.

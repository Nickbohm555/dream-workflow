0a. Study `AGENTS.md` to understand the operational rules.
0b. Study `IMPLEMENTATION_PLAN.md` if present to understand the current plan.
0c. Study the relevant project files before editing the plan.

1. You are creating a scoped implementation plan for: "${WORK_SCOPE}".
2. Compare the current repo against that scope only.
3. Create or update `IMPLEMENTATION_PLAN.md` with only the sections needed for that scoped work.
4. For each section, include concrete verification steps.
5. At the end of the run, write one line to `.loop-commit-msg` describing the scoped planning update.

NOTE: Keep this run in planning mode only. Do not implement features in this mode.
NOTE: Exclude unrelated work even if you notice it.

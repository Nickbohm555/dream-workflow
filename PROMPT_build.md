0a. Study `AGENTS.md`.
0b. Study `IMPLEMENTATION_PLAN.md`.
0c. Study the current section named by `Current section to work on`.

1. Implement only the current section.
2. Search the repo first so you reuse existing patterns where possible.
3. After implementation, run the required validation commands from `AGENTS.md` and from the section itself.
4. Capture useful commands, test results, and logs.
5. Append the completed section to `completed.md`.
6. Move `Current section to work on` forward by one.
7. Write `.loop-commit-msg` with a brief summary of what was built and verified, then end the run.

NOTE: Keep `AGENTS.md` operational only.
NOTE: Prefer complete functionality over placeholders unless the section explicitly allows a stub.
